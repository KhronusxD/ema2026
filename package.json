{
  "name": "ema-2026---expandindo-o-reino",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:main": "vite build",
    "build:pc": "cd pc-live-quinta && npm install --no-fund --no-audit && npm run build && mkdir -p ../dist/pc-live-quinta && cp -a dist/. ../dist/pc-live-quinta/",
    "build": "npm run build:main && npm run build:pc",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "lucide-react": "^0.563.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}